# 验收记录

日期：2026-09-12。环境：Windows、Node.js 22.22.0、本机回环服务。旧 `server` 源码、历史、密钥、模型及虚拟环境均未修改。

## 自动检查结果

| 检查 | 结果 |
| --- | --- |
| 前端格式检查、类型检查 | 通过 |
| 后端格式检查、类型检查 | 通过 |
| 前端单元测试 | 21 项通过 |
| 后端接口/数据/模型解析测试 | 10 项通过 |
| Playwright 端到端测试 | 13 项通过 |
| 前端、后端生产构建 | 通过 |
| 生产后端健康检查 | 编译产物运行返回 200 / status=ok |
| Vite 生产预览 | 产物 `/editor` 返回 200；临时 4176 预览进程已关闭 |
| npm 依赖审计 | 前后端均为 0 个已知漏洞 |
| Git 空白检查 | 通过（含尚未暂存的新源码） |

前端开发服务使用 4174，后端使用 3002。端到端测试使用独立 4175 / 3003 临时服务，不依赖旧应用、不读取正式密钥。后端以注入的模型响应验证传入模型、分析尺寸、源像素映射、空/异常/超过 200 个候选，未调用真实计费服务。

## 已覆盖的关键行为

- 文件导入、粘贴、拖放、EXIF 方向、真实格式与大小校验。
- 场景新建和切换、图层排序、显隐、锁定、单选/多选、复制、对齐、删除。
- 画布拖动、控制点缩放、旋转/翻转参数、平移缩放不增加撤销步；编辑撤销/重做。
- 整图识别、旋转/翻转后的局部框选、候选命名和尺寸编辑、手动候选、批量创建一个撤销步骤。
- 原图不变、裁片原像素一致、世界位置和比例保持、PNG 原密度与 ZIP/场景输出。
- 取消后的迟到结果、来源变更、保存失败后重试、多标签版本冲突、失败回滚与幂等重复提交。
- 独立模型设置、模型列表、主动测试、响应脱敏、Origin/Host 限制及 OpenAPI 请求契约。
- 1920×1080、1280×800 桌面；800×600、390×844 互斥抽屉、焦点返回与无页面横向溢出。

## 视觉记录

已人工查看参考站当前登录页面，以及以下本地截图；修正了默认蓝色组件残留、中文参数换行、图标按钮事件和窄屏焦点问题。测试图由 Canvas 在本地合成，不复制参考站的私有设计图素材。

- [1920 桌面](screenshots/editor-1920.png)
- [1280 桌面](screenshots/editor-1280.png)
- [800 窄屏](screenshots/editor-800.png) / [属性抽屉](screenshots/drawer-800.png)
- [390 手机宽度](screenshots/editor-390.png) / [属性抽屉](screenshots/drawer-390.png)
- [旋转、翻转及 AI 候选](screenshots/ai-candidates.png)
- 四种尺寸的空场景截图同目录以 `empty-` 开头。

## 验收边界

这是已确认范围内的独立实现，不是原站全功能或逐像素截图复制。参考站当前窗口的人工对照不等同于两站在每个目标分辨率下的自动像素差异验收；会员、协作等已省略，顶部操作数量与原站不同。

真实图片理解模型尚未调用，需要用户输入配置并主动确认费用后验证实际服务兼容性和识别质量。触屏手势与多点捏合不是本轮自动化验收内容；手机宽度测试验证的是布局、抽屉和操作入口。会话撤销栈、尚未应用候选不跨刷新保存。

## 后续维护

运行步骤见 `README.md`，模块职责与不可破坏的约束见 [代码维护指南](architecture.md)。后续修改须继续通过两个工程的 `format:check`，避免重新引入单行压缩源码。
